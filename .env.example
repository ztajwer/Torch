# Copy to .env and fill in your Gemini API key (get one at https://aistudio.google.com/apikey)
TORCH_GEMINI_API_KEY=AIzaSyCXEqUEdNY6x1xWWVqggT5GrEWIHTZLykI
TORCH_GEMINI_MODEL=gemini-2.0-flash

# Local dev only (defaults are fine)
# TORCH_CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Production Docker / Render (set in host dashboard, not usually in local .env)
# TORCH_SERVE_STATIC=true
# TORCH_ENVIRONMENT=production
# TORCH_CORS_ORIGINS=https://your-app.onrender.com
